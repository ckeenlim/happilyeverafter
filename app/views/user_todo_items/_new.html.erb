<%= simple_form_for @user_todo_item, 
		:url => url_for(:action => 'create', :controller => 'user_todo_items'),
   	:method => 'post',
		remote: true do |f| %>
	<%= f.input :item, label: 'Your Own New Item', class: "new-item-input"  %>
	<%= f.button :submit, 'Add' %>
<% end %>
